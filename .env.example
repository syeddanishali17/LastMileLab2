BACKEND_URL=http://localhost:8000
CORS_ORIGINS=http://localhost:8501
LOG_LEVEL=INFO
DUCKDB_PATH=data/runs/lastmile.duckdb

# Docker Compose: the frontend container uses BACKEND_URL=http://backend:8000.
# The browser still opens http://localhost:8501, so keep CORS_ORIGINS on that origin.
